{
  "name": "url-shortener",
  "version": "1.0.0",
  "private": true,
  "description": "Simple URL shortener (Node.js + Express) with Jenkins + AWS deployment",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=20"
  }
}
